{
  "name": "op_agent",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "轻量化 Linux 运维 Agent —— 基于 pi coding agent SDK",
  "bin": {
    "opagent": "./src/index.ts"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --hot run src/index.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "^0.80.6",
    "typebox": "^1.1.38"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
